# Boilerplate for IIFE

```js
/**
 * A boilerplate for Immediately Invoked Function Expression using ES6.
 *
 * @param {Object} root - Represents a window containing a DOM document
 */
((root) => {

  // Add code here...

})(window);
```

```js
/**
 * A boilerplate for Immediately Invoked Function Expression using ES5.
 *
 * @param {Object} root - Represents a window containing a DOM document
 */
(function (root) {

    'use strict';

    // Add code here...

})(window);
```
